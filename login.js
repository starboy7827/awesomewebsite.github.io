// script.js

// Example of adding dynamic graphics or animations
document.addEventListener('DOMContentLoaded', () => {
    const backgroundOverlay = document.querySelector('.background-overlay');
    backgroundOverlay.style.animation = 'fadeIn 2s ease-in-out';

    const loginBox = document.querySelector('.login-box');
    loginBox.style.animation = 'popIn 1s ease-in-out';
});
